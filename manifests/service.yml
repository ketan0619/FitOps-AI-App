kind: Service
apiVersion: v1
metadata:
     name: fitops-service
spec:
     type: NodePort
     selector:
           app: fitops
     ports:
     - port: 80
       targetPort: 80
       nodePort: 30007
