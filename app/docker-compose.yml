services:
  mysql:
    image: mysql:8.0
    container_name: fitops-mysql
    environment:
      MYSQL_ROOT_PASSWORD: fitops123
      MYSQL_DATABASE: fitopsdb
      MYSQL_USER: fitops
      MYSQL_PASSWORD: fitops123
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - fitops-net

  ollama:
    image: ollama/ollama
    container_name: fitops-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - fitops-net

  ollama-pull-model:
    image: ollama/ollama
    container_name: ollama-pull-model
    environment:
      - OLLAMA_HOST=ollama
    volumes:
      - ollama-data:/root/.ollama
    depends_on:
      ollama:
        condition: service_healthy
    entrypoint: /bin/sh
    command: -c "ollama pull tinyllama"
    networks:
      - fitops-net

  fitops-ai-app:
    build: .
    container_name: fitops
    ports:
      - "8080:8080"
    environment:
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_DATABASE: fitopsdb
      MYSQL_USER: root
      MYSQL_PASSWORD: fitops123
      OLLAMA_URL: http://ollama:11434
    depends_on:
      mysql:
        condition: service_healthy
      ollama-pull-model:
        condition: service_completed_successfully
    networks:
      - fitops-net

volumes:
  mysql-data:
  ollama-data:

networks:
  fitops-net:
    driver: bridge
